"use strict";

const assert = require('chai').assert;

describe('controllers:UserController', () => {
  it('Should list all objects');
  it('given a valid user id, it should list a single user');
});
