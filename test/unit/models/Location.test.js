"use strict";

const assert = require('chai').assert;

const newLocation = {
  name: 'Test location'
};

describe('models:Location', () => {
  it('Should create new location');
  it('Should remove a location');
  it('Should find a list of locations');
  it('Should find a single location');
});
