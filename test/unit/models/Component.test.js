"use strict";

const assert = require('chai').assert;

const newComponent = {
  name: 'Test component'
};

describe('models:Component', () => {
  it('Should create new component');
  it('Should remove a component');
  it('Should find a list of components');
  it('Should find a single component');
});
